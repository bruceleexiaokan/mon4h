<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>mon4h.framework.dashboard</groupId>
        <artifactId>mon4h-dashboard-parent</artifactId>
        <version>1.0.0</version>
    </parent>
	
    <groupId>mon4h.framework.dashboard</groupId>
    <artifactId>mon4h-dashboard-io-servlet</artifactId>
    <packaging>war</packaging>
    <version>${mon4h-dashboard.version}</version>
    <name>mon4h-dashboard-io-servlet</name>

    <properties>
        <web.context.name>dashboard-io</web.context.name>
    </properties>

    <dependencies>
        <dependency>
            <groupId>mon4h.framework.dashboard</groupId>
            <artifactId>mon4h-dashboard-common</artifactId>
			<version>${mon4h-dashboard.version}</version>
        </dependency>
        <dependency>
            <groupId>mon4h.framework.dashboard</groupId>
            <artifactId>mon4h-dashboard-engine</artifactId>
			<version>${mon4h-dashboard.version}</version>
            <scope>runtime</scope>
            <exclusions>
                <exclusion>
                    <groupId>javax.servlet</groupId>
                    <artifactId>servlet-api</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.mortbay.jetty</groupId>
                    <artifactId>servlet-api-2.5</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
        </dependency>
        <dependency>
            <groupId>mon4h.framework.dashboard</groupId>
            <artifactId>mon4h-dashboard-assembly</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource>
                <directory>${project.basedir}/src/main/resources</directory>
            </resource>
            <resource>
                <directory>${project.build.directory}/generated-resources/common</directory>
                <filtering>true</filtering>
            </resource>
            <resource>
                <directory>${project.build.directory}/generated-resources/environment/${package.environment}</directory>
                <includes>
                    <include>dashboard-db*</include>
                    <include>dashboard-engine*</include>
                    <include>logback*</include>
                </includes>
                <filtering>true</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>unpack-shared-resources</id>
                        <goals>
                            <goal>unpack-dependencies</goal>
                        </goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <includeGroupIds>${project.groupId}</includeGroupIds>
                            <includeArtifactIds>mon4h-dashboard-assembly</includeArtifactIds>
                            <outputDirectory>${project.build.directory}/generated-resources</outputDirectory>
                            <excludeTransitive>true</excludeTransitive>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.mortbay.jetty</groupId>
                <artifactId>maven-jetty-plugin</artifactId>
                <configuration>
                    <scanIntervalSeconds>5</scanIntervalSeconds>
                    <connectors>
                        <connector implementation="org.mortbay.jetty.nio.SelectChannelConnector">
                            <port>9090</port>
                            <maxIdleTime>60000</maxIdleTime>
                        </connector>
                    </connectors>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-war-plugin</artifactId>
                <version>2.1.1</version>
                <configuration>
                    <packagingExcludes>WEB-INF/web.xml</packagingExcludes>
                    <warName>${web.context.name}-${project.version}-${package.environment}-${build.time}</warName>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
